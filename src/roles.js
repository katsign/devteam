function genManager(manager) {
    return ``;
}

function genEngineer(engineer) {
    return ``;
}

function genIntern(intern) {
    return ``;
}

module.exports = {
    mHTML: genManager,
    eHTML: genEngineer,
    iHTML: genIntern
}